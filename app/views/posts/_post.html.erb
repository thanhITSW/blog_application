<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>
  <h6>Posted by: <%= link_to post.user.name, user_path(post.user) %></h6>
  <h6>
    <%= pluralize(post.views, "view") %>
  </h6>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
